This file wants random additions and deletion to make a wierd conflict.
A piece of C snippet as follows:
// If node2 is the head node
    if (prevNode2 == NULL)
    {
        *head = node1;
    }
    else
    {
        prevNode2->next = node1;
    }

    // Swap the next pointers of node1 and node2
    Node_t *temp = node1->next;
    node1->next = node2->next;
    node2->next = temp;

A mini Makefile, if all was complete:
all:
	gcc main.c -g -o main.prog
	./main.prog

EDIT MORE...